project_id = "gcwuvijcuzhunkcauzom"

[functions.og-image]
verify_jwt = false

[functions.og-profile]
verify_jwt = false

[functions.moderate-content]
verify_jwt = true

# Webhooks are called by external services with their own secret header,
# NOT a Supabase JWT — the gateway must not require a JWT or it rejects them.
[functions.revenuecat-webhook]
verify_jwt = false
